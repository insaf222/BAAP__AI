services:
  - type: web
    name: baap-ai
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: DISCORD_TOKEN
        sync: false